import Engine from "./Engine";
import IEngine from "./IEngine";
import display from "./display";

class BrokenEngine implements IEngine {
  constructor() {
    // calls constructor of parent class
  }
}

export default BrokenEngine;
